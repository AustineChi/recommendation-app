import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    errorText: {
        color: 'red',
        fontSize: 14,
        marginVertical: 5,
        marginLeft: 10,
    },
});
