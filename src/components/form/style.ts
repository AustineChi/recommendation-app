import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    formContainer: {
        padding: 20,
    },
    input: {
        marginBottom: 15,
    },
    button: {
        marginTop: 10,
    },
    errorText: {
        color: 'red',
        marginBottom: 15,
    },
});
